{
  "name": "agents",
  "private": true,
  "type": "module",
  "scripts": {
    "ruler:apply": "ruler apply",
    "ruler:revert": "ruler revert",
    "render": "bun src/cli.ts",
    "clean": "rm -rf out",
    "lint": "oxlint --fix",
    "format": "oxfmt --write",
    "check": "bun run --parallel lint format"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.12.0"
  },
  "devDependencies": {
    "@intellectronica/ruler": "^0.3.38",
    "@types/bun": "latest",
    "oxfmt": "^0.46.0",
    "oxlint": "^1.61.0",
    "oxlint-tsgolint": "^0.21.1"
  },
  "peerDependencies": {
    "typescript": "^6"
  }
}
